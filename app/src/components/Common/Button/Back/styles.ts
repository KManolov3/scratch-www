import { StyleSheet } from 'react-native';
import { scale } from '../../../../styles/base';

export const iconHeigh = scale(20);
export const iconWidth = scale(12);

export default StyleSheet.create({
  backButton: {
    paddingHorizontal: scale(8),
  },
});
