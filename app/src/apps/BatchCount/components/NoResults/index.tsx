import { Container } from '@components/Container';
import { Text } from '@components/Text';

export function NoResults() {
  return (
    <Container>
      <Text>No results found for your search. Please try again.</Text>
    </Container>
  );
}
